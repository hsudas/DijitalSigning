@model osVodigiWeb6x.Models.Music

@{
    ViewBag.Title = "Edit";
}

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
<script type="text/javascript">
    $(function () {
        $("input:text:visible:first").focus();
    });
</script>

<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        <img src="~/Images/music-icon.png" alt="" style="border-radius:8px 8px;width:50px" />
        Müzik Düzenle
        <small></small>
    </h1>
    <ol class="breadcrumb">
        @Html.ActionLink("Müzik Ekle", "Create")
        @*<li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
            <li><a href="#">Tables</a></li>
            <li class="active">Simple</li>*@
    </ol>
</section>
@using (Html.BeginForm())
{
    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box box-danger">
                    <div class="box-header">
                        <h3 class="box-title"></h3>
                    </div>
                    <div class="box-body">
                        <div class="row">
                            <div class="col-xs-3">
                                <div class="form-group">
                                    <label>Resim</label><span class="required">*</span>
                                    @Html.TextBoxFor(model => model.MusicName, new { @class = "form-control", maxlength = 128 })
                                </div>
                            </div>
                            <div class="col-xs-3">
                                <label for="exampleInputEmail1">Aktif</label>
                                <br />
                                @Html.EditorFor(model => model.IsActive)
                            </div>
                            <div class="col-xs-2"></div>
                        </div>                        
                    </div><!-- /.box-body -->

                    <div class="box-footer" style="text-align:center">
                        <button type="submit" class="btn btn-primary" onclick="ResetPageCount()">Kaydet</button>
                    </div>

                </div><!-- /.box -->
            </div>
        </div>
    </section>
    <div class="errormessage">@Html.Raw((string)ViewData["ValidationMessage"])</div>
    @Html.HiddenFor(model => model.MusicID)
    @Html.HiddenFor(model => model.AccountID)
    @Html.HiddenFor(model => model.OriginalFilename)
    @Html.HiddenFor(model => model.StoredFilename)
}
